{
	"name": "create-release",
	"version": "1.8.0",
	"description": "GitHub Workflows to create and publish releases with semantic versioning",
	"type": "module",
	"engines": {
		"node": ">=24.0.0"
	},
	"scripts": {
		"build": "echo to produce the CHANGELOG run: 'npm install @semantic-release/changelog -D'"
	},
	"keywords": [
		"github",
		"actions"
	],
	"devDependencies": {
		"@semantic-release/changelog": "^6.0.0",
		"@semantic-release/git": "^10.0.0",
		"@semantic-release/npm": "^12.0.0",
		"semantic-release": "^24.0.0",
		"semantic-release-export-data": "^1.0.0"
	},
	"overrides": {
		"picomatch": "^4.0.4",
		"undici": "^6.23.0"
	}
}
